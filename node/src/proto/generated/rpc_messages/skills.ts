// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: rpc_messages/skills.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AgentToolResult, AgentUsage } from "../agent_messages";

export const protobufPackage = "terminal";

/** SkillInfo describes an installed skill. */
export interface SkillInfo {
  /** Skill name (lowercase-with-hyphens, e.g. "code-review") */
  name: string;
  /** Human-readable description */
  description: string;
  /** Skill author */
  author: string;
  /** Semantic version (e.g. "1.0.0") */
  version: string;
  /** LLM model override (e.g. "@smart+agents", "claude-opus-4-6") */
  model: string;
  /** Where the skill was loaded from: "builtin", "global", or "workspace" */
  origin: string;
  /** Required system binaries (e.g. ["git", "kubectl"]) */
  requiredBins: string[];
  /** Required environment variables (e.g. ["GITHUB_TOKEN"]) */
  requiredEnv: string[];
}

/** Request to list all installed skills on the connected machine. */
export interface SkillListRequest {
  /** Session ID with connected agent (required for remote, optional for local) */
  sessionId: string;
}

/** Response with list of installed skills. */
export interface SkillListResponse {
  /** All installed skills (builtin + global + workspace) */
  skills: SkillInfo[];
  /** Error message if listing failed (e.g. no active session) */
  error: string;
}

/** Request to get details of a specific skill. */
export interface SkillShowRequest {
  /** Session ID with connected agent (required for remote, optional for local) */
  sessionId: string;
  /** Skill name to look up (required) */
  skillName: string;
}

/** Response with skill details. */
export interface SkillShowResponse {
  /** Whether the skill was found */
  found: boolean;
  /** Skill metadata (only set if found=true) */
  info:
    | SkillInfo
    | undefined;
  /** System prompt body (markdown content) */
  content: string;
  /** Source file path on the machine */
  source: string;
  /** Error message if lookup failed */
  error: string;
}

/** Request to execute a skill with a prompt. */
export interface SkillRunRequest {
  /** Session ID with connected agent (required for remote, optional for local) */
  sessionId: string;
  /** Unique request ID for correlation (optional, generated if empty) */
  requestId: string;
  /** Skill name to run (required) */
  skillName: string;
  /** User prompt/question for the skill (required) */
  prompt: string;
  /** Additional configuration options (e.g. model override) */
  options: { [key: string]: string };
  /** Maximum execution time in seconds (default: 300, max: 600) */
  timeoutSeconds: number;
  /**
   * Optional: JSON Schema for structured output
   * If provided, skill will return output_json instead of text
   */
  outputSchema: string;
}

export interface SkillRunRequest_OptionsEntry {
  key: string;
  value: string;
}

/** Response from skill execution. */
export interface SkillRunResponse {
  /** Request ID for correlation (matches SkillRunRequest.request_id) */
  requestId: string;
  /** Whether execution succeeded */
  success: boolean;
  /** Skill's response text */
  text: string;
  /** Error message if !success */
  error: string;
  /** Tool execution results (if skill used tools) */
  toolResults: AgentToolResult[];
  /** Token usage statistics */
  usage:
    | AgentUsage
    | undefined;
  /** Execution duration in milliseconds */
  durationMs: string;
  /** Structured output JSON (if output_schema was provided in request) */
  outputJson: string;
}

function createBaseSkillInfo(): SkillInfo {
  return {
    name: "",
    description: "",
    author: "",
    version: "",
    model: "",
    origin: "",
    requiredBins: [],
    requiredEnv: [],
  };
}

export const SkillInfo: MessageFns<SkillInfo> = {
  encode(message: SkillInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.author !== "") {
      writer.uint32(26).string(message.author);
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    if (message.model !== "") {
      writer.uint32(42).string(message.model);
    }
    if (message.origin !== "") {
      writer.uint32(50).string(message.origin);
    }
    for (const v of message.requiredBins) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.requiredEnv) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.model = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.origin = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.requiredBins.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.requiredEnv.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      model: isSet(object.model) ? globalThis.String(object.model) : "",
      origin: isSet(object.origin) ? globalThis.String(object.origin) : "",
      requiredBins: globalThis.Array.isArray(object?.requiredBins)
        ? object.requiredBins.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.required_bins)
        ? object.required_bins.map((e: any) => globalThis.String(e))
        : [],
      requiredEnv: globalThis.Array.isArray(object?.requiredEnv)
        ? object.requiredEnv.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.required_env)
        ? object.required_env.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SkillInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    if (message.origin !== "") {
      obj.origin = message.origin;
    }
    if (message.requiredBins?.length) {
      obj.requiredBins = message.requiredBins;
    }
    if (message.requiredEnv?.length) {
      obj.requiredEnv = message.requiredEnv;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillInfo>): SkillInfo {
    return SkillInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillInfo>): SkillInfo {
    const message = createBaseSkillInfo();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.author = object.author ?? "";
    message.version = object.version ?? "";
    message.model = object.model ?? "";
    message.origin = object.origin ?? "";
    message.requiredBins = object.requiredBins?.map((e) => e) || [];
    message.requiredEnv = object.requiredEnv?.map((e) => e) || [];
    return message;
  },
};

function createBaseSkillListRequest(): SkillListRequest {
  return { sessionId: "" };
}

export const SkillListRequest: MessageFns<SkillListRequest> = {
  encode(message: SkillListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillListRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: SkillListRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillListRequest>): SkillListRequest {
    return SkillListRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillListRequest>): SkillListRequest {
    const message = createBaseSkillListRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseSkillListResponse(): SkillListResponse {
  return { skills: [], error: "" };
}

export const SkillListResponse: MessageFns<SkillListResponse> = {
  encode(message: SkillListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.skills) {
      SkillInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skills.push(SkillInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillListResponse {
    return {
      skills: globalThis.Array.isArray(object?.skills) ? object.skills.map((e: any) => SkillInfo.fromJSON(e)) : [],
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: SkillListResponse): unknown {
    const obj: any = {};
    if (message.skills?.length) {
      obj.skills = message.skills.map((e) => SkillInfo.toJSON(e));
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillListResponse>): SkillListResponse {
    return SkillListResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillListResponse>): SkillListResponse {
    const message = createBaseSkillListResponse();
    message.skills = object.skills?.map((e) => SkillInfo.fromPartial(e)) || [];
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseSkillShowRequest(): SkillShowRequest {
  return { sessionId: "", skillName: "" };
}

export const SkillShowRequest: MessageFns<SkillShowRequest> = {
  encode(message: SkillShowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.skillName !== "") {
      writer.uint32(18).string(message.skillName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillShowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillShowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.skillName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillShowRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      skillName: isSet(object.skillName)
        ? globalThis.String(object.skillName)
        : isSet(object.skill_name)
        ? globalThis.String(object.skill_name)
        : "",
    };
  },

  toJSON(message: SkillShowRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.skillName !== "") {
      obj.skillName = message.skillName;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillShowRequest>): SkillShowRequest {
    return SkillShowRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillShowRequest>): SkillShowRequest {
    const message = createBaseSkillShowRequest();
    message.sessionId = object.sessionId ?? "";
    message.skillName = object.skillName ?? "";
    return message;
  },
};

function createBaseSkillShowResponse(): SkillShowResponse {
  return { found: false, info: undefined, content: "", source: "", error: "" };
}

export const SkillShowResponse: MessageFns<SkillShowResponse> = {
  encode(message: SkillShowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    if (message.info !== undefined) {
      SkillInfo.encode(message.info, writer.uint32(18).fork()).join();
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    if (message.source !== "") {
      writer.uint32(34).string(message.source);
    }
    if (message.error !== "") {
      writer.uint32(42).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillShowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillShowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.info = SkillInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillShowResponse {
    return {
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      info: isSet(object.info) ? SkillInfo.fromJSON(object.info) : undefined,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: SkillShowResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.info !== undefined) {
      obj.info = SkillInfo.toJSON(message.info);
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillShowResponse>): SkillShowResponse {
    return SkillShowResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillShowResponse>): SkillShowResponse {
    const message = createBaseSkillShowResponse();
    message.found = object.found ?? false;
    message.info = (object.info !== undefined && object.info !== null) ? SkillInfo.fromPartial(object.info) : undefined;
    message.content = object.content ?? "";
    message.source = object.source ?? "";
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseSkillRunRequest(): SkillRunRequest {
  return { sessionId: "", requestId: "", skillName: "", prompt: "", options: {}, timeoutSeconds: 0, outputSchema: "" };
}

export const SkillRunRequest: MessageFns<SkillRunRequest> = {
  encode(message: SkillRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    if (message.skillName !== "") {
      writer.uint32(26).string(message.skillName);
    }
    if (message.prompt !== "") {
      writer.uint32(34).string(message.prompt);
    }
    globalThis.Object.entries(message.options).forEach(([key, value]: [string, string]) => {
      SkillRunRequest_OptionsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    if (message.timeoutSeconds !== 0) {
      writer.uint32(48).int32(message.timeoutSeconds);
    }
    if (message.outputSchema !== "") {
      writer.uint32(58).string(message.outputSchema);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.skillName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = SkillRunRequest_OptionsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.options[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.timeoutSeconds = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.outputSchema = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillRunRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      skillName: isSet(object.skillName)
        ? globalThis.String(object.skillName)
        : isSet(object.skill_name)
        ? globalThis.String(object.skill_name)
        : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      options: isObject(object.options)
        ? (globalThis.Object.entries(object.options) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      timeoutSeconds: isSet(object.timeoutSeconds)
        ? globalThis.Number(object.timeoutSeconds)
        : isSet(object.timeout_seconds)
        ? globalThis.Number(object.timeout_seconds)
        : 0,
      outputSchema: isSet(object.outputSchema)
        ? globalThis.String(object.outputSchema)
        : isSet(object.output_schema)
        ? globalThis.String(object.output_schema)
        : "",
    };
  },

  toJSON(message: SkillRunRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.skillName !== "") {
      obj.skillName = message.skillName;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.options) {
      const entries = globalThis.Object.entries(message.options) as [string, string][];
      if (entries.length > 0) {
        obj.options = {};
        entries.forEach(([k, v]) => {
          obj.options[k] = v;
        });
      }
    }
    if (message.timeoutSeconds !== 0) {
      obj.timeoutSeconds = Math.round(message.timeoutSeconds);
    }
    if (message.outputSchema !== "") {
      obj.outputSchema = message.outputSchema;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillRunRequest>): SkillRunRequest {
    return SkillRunRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillRunRequest>): SkillRunRequest {
    const message = createBaseSkillRunRequest();
    message.sessionId = object.sessionId ?? "";
    message.requestId = object.requestId ?? "";
    message.skillName = object.skillName ?? "";
    message.prompt = object.prompt ?? "";
    message.options = (globalThis.Object.entries(object.options ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.timeoutSeconds = object.timeoutSeconds ?? 0;
    message.outputSchema = object.outputSchema ?? "";
    return message;
  },
};

function createBaseSkillRunRequest_OptionsEntry(): SkillRunRequest_OptionsEntry {
  return { key: "", value: "" };
}

export const SkillRunRequest_OptionsEntry: MessageFns<SkillRunRequest_OptionsEntry> = {
  encode(message: SkillRunRequest_OptionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillRunRequest_OptionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillRunRequest_OptionsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillRunRequest_OptionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SkillRunRequest_OptionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillRunRequest_OptionsEntry>): SkillRunRequest_OptionsEntry {
    return SkillRunRequest_OptionsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillRunRequest_OptionsEntry>): SkillRunRequest_OptionsEntry {
    const message = createBaseSkillRunRequest_OptionsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSkillRunResponse(): SkillRunResponse {
  return {
    requestId: "",
    success: false,
    text: "",
    error: "",
    toolResults: [],
    usage: undefined,
    durationMs: "0",
    outputJson: "",
  };
}

export const SkillRunResponse: MessageFns<SkillRunResponse> = {
  encode(message: SkillRunResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.error !== "") {
      writer.uint32(34).string(message.error);
    }
    for (const v of message.toolResults) {
      AgentToolResult.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.usage !== undefined) {
      AgentUsage.encode(message.usage, writer.uint32(50).fork()).join();
    }
    if (message.durationMs !== "0") {
      writer.uint32(56).int64(message.durationMs);
    }
    if (message.outputJson !== "") {
      writer.uint32(66).string(message.outputJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillRunResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillRunResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.toolResults.push(AgentToolResult.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.usage = AgentUsage.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.durationMs = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.outputJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillRunResponse {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      toolResults: globalThis.Array.isArray(object?.toolResults)
        ? object.toolResults.map((e: any) => AgentToolResult.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_results)
        ? object.tool_results.map((e: any) => AgentToolResult.fromJSON(e))
        : [],
      usage: isSet(object.usage) ? AgentUsage.fromJSON(object.usage) : undefined,
      durationMs: isSet(object.durationMs)
        ? globalThis.String(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.String(object.duration_ms)
        : "0",
      outputJson: isSet(object.outputJson)
        ? globalThis.String(object.outputJson)
        : isSet(object.output_json)
        ? globalThis.String(object.output_json)
        : "",
    };
  },

  toJSON(message: SkillRunResponse): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.toolResults?.length) {
      obj.toolResults = message.toolResults.map((e) => AgentToolResult.toJSON(e));
    }
    if (message.usage !== undefined) {
      obj.usage = AgentUsage.toJSON(message.usage);
    }
    if (message.durationMs !== "0") {
      obj.durationMs = message.durationMs;
    }
    if (message.outputJson !== "") {
      obj.outputJson = message.outputJson;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillRunResponse>): SkillRunResponse {
    return SkillRunResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillRunResponse>): SkillRunResponse {
    const message = createBaseSkillRunResponse();
    message.requestId = object.requestId ?? "";
    message.success = object.success ?? false;
    message.text = object.text ?? "";
    message.error = object.error ?? "";
    message.toolResults = object.toolResults?.map((e) => AgentToolResult.fromPartial(e)) || [];
    message.usage = (object.usage !== undefined && object.usage !== null)
      ? AgentUsage.fromPartial(object.usage)
      : undefined;
    message.durationMs = object.durationMs ?? "0";
    message.outputJson = object.outputJson ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
