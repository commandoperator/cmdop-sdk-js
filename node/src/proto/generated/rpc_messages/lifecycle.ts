// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: rpc_messages/lifecycle.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "terminal";

/** Enter background mode (app goes to background) */
export interface EnterBackgroundRequest {
  sessionId: string;
}

export interface EnterBackgroundResponse {
  success: boolean;
  error: string;
}

/** Wake from background (app wakes from push) */
export interface WakeFromBackgroundRequest {
  sessionId: string;
}

export interface WakeFromBackgroundResponse {
  success: boolean;
  error: string;
  pendingCommands: PendingCommand[];
}

/** Pending command structure */
export interface PendingCommand {
  commandId: string;
  /** "input", "resize", "signal", etc. */
  type: string;
  /** Command data */
  data: Buffer;
  /** Unix timestamp */
  queuedAt: string;
}

/** Queue command for background session */
export interface QueueCommandRequest {
  sessionId: string;
  command: PendingCommand | undefined;
}

export interface QueueCommandResponse {
  queued: boolean;
  pushSent: boolean;
  /** Current session status */
  status: string;
  error: string;
}

function createBaseEnterBackgroundRequest(): EnterBackgroundRequest {
  return { sessionId: "" };
}

export const EnterBackgroundRequest: MessageFns<EnterBackgroundRequest> = {
  encode(message: EnterBackgroundRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnterBackgroundRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnterBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnterBackgroundRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: EnterBackgroundRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<EnterBackgroundRequest>): EnterBackgroundRequest {
    return EnterBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EnterBackgroundRequest>): EnterBackgroundRequest {
    const message = createBaseEnterBackgroundRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseEnterBackgroundResponse(): EnterBackgroundResponse {
  return { success: false, error: "" };
}

export const EnterBackgroundResponse: MessageFns<EnterBackgroundResponse> = {
  encode(message: EnterBackgroundResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnterBackgroundResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnterBackgroundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnterBackgroundResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: EnterBackgroundResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<EnterBackgroundResponse>): EnterBackgroundResponse {
    return EnterBackgroundResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EnterBackgroundResponse>): EnterBackgroundResponse {
    const message = createBaseEnterBackgroundResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseWakeFromBackgroundRequest(): WakeFromBackgroundRequest {
  return { sessionId: "" };
}

export const WakeFromBackgroundRequest: MessageFns<WakeFromBackgroundRequest> = {
  encode(message: WakeFromBackgroundRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WakeFromBackgroundRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWakeFromBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WakeFromBackgroundRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: WakeFromBackgroundRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<WakeFromBackgroundRequest>): WakeFromBackgroundRequest {
    return WakeFromBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WakeFromBackgroundRequest>): WakeFromBackgroundRequest {
    const message = createBaseWakeFromBackgroundRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseWakeFromBackgroundResponse(): WakeFromBackgroundResponse {
  return { success: false, error: "", pendingCommands: [] };
}

export const WakeFromBackgroundResponse: MessageFns<WakeFromBackgroundResponse> = {
  encode(message: WakeFromBackgroundResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    for (const v of message.pendingCommands) {
      PendingCommand.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WakeFromBackgroundResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWakeFromBackgroundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pendingCommands.push(PendingCommand.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WakeFromBackgroundResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      pendingCommands: globalThis.Array.isArray(object?.pendingCommands)
        ? object.pendingCommands.map((e: any) => PendingCommand.fromJSON(e))
        : globalThis.Array.isArray(object?.pending_commands)
        ? object.pending_commands.map((e: any) => PendingCommand.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WakeFromBackgroundResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.pendingCommands?.length) {
      obj.pendingCommands = message.pendingCommands.map((e) => PendingCommand.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<WakeFromBackgroundResponse>): WakeFromBackgroundResponse {
    return WakeFromBackgroundResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WakeFromBackgroundResponse>): WakeFromBackgroundResponse {
    const message = createBaseWakeFromBackgroundResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    message.pendingCommands = object.pendingCommands?.map((e) => PendingCommand.fromPartial(e)) || [];
    return message;
  },
};

function createBasePendingCommand(): PendingCommand {
  return { commandId: "", type: "", data: Buffer.alloc(0), queuedAt: "0" };
}

export const PendingCommand: MessageFns<PendingCommand> = {
  encode(message: PendingCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.queuedAt !== "0") {
      writer.uint32(32).int64(message.queuedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PendingCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePendingCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.queuedAt = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PendingCommand {
    return {
      commandId: isSet(object.commandId)
        ? globalThis.String(object.commandId)
        : isSet(object.command_id)
        ? globalThis.String(object.command_id)
        : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0),
      queuedAt: isSet(object.queuedAt)
        ? globalThis.String(object.queuedAt)
        : isSet(object.queued_at)
        ? globalThis.String(object.queued_at)
        : "0",
    };
  },

  toJSON(message: PendingCommand): unknown {
    const obj: any = {};
    if (message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.queuedAt !== "0") {
      obj.queuedAt = message.queuedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<PendingCommand>): PendingCommand {
    return PendingCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PendingCommand>): PendingCommand {
    const message = createBasePendingCommand();
    message.commandId = object.commandId ?? "";
    message.type = object.type ?? "";
    message.data = object.data ?? Buffer.alloc(0);
    message.queuedAt = object.queuedAt ?? "0";
    return message;
  },
};

function createBaseQueueCommandRequest(): QueueCommandRequest {
  return { sessionId: "", command: undefined };
}

export const QueueCommandRequest: MessageFns<QueueCommandRequest> = {
  encode(message: QueueCommandRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.command !== undefined) {
      PendingCommand.encode(message.command, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueCommandRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueCommandRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command = PendingCommand.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueCommandRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      command: isSet(object.command) ? PendingCommand.fromJSON(object.command) : undefined,
    };
  },

  toJSON(message: QueueCommandRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.command !== undefined) {
      obj.command = PendingCommand.toJSON(message.command);
    }
    return obj;
  },

  create(base?: DeepPartial<QueueCommandRequest>): QueueCommandRequest {
    return QueueCommandRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueueCommandRequest>): QueueCommandRequest {
    const message = createBaseQueueCommandRequest();
    message.sessionId = object.sessionId ?? "";
    message.command = (object.command !== undefined && object.command !== null)
      ? PendingCommand.fromPartial(object.command)
      : undefined;
    return message;
  },
};

function createBaseQueueCommandResponse(): QueueCommandResponse {
  return { queued: false, pushSent: false, status: "", error: "" };
}

export const QueueCommandResponse: MessageFns<QueueCommandResponse> = {
  encode(message: QueueCommandResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queued !== false) {
      writer.uint32(8).bool(message.queued);
    }
    if (message.pushSent !== false) {
      writer.uint32(16).bool(message.pushSent);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.error !== "") {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueCommandResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueCommandResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.queued = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pushSent = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueCommandResponse {
    return {
      queued: isSet(object.queued) ? globalThis.Boolean(object.queued) : false,
      pushSent: isSet(object.pushSent)
        ? globalThis.Boolean(object.pushSent)
        : isSet(object.push_sent)
        ? globalThis.Boolean(object.push_sent)
        : false,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: QueueCommandResponse): unknown {
    const obj: any = {};
    if (message.queued !== false) {
      obj.queued = message.queued;
    }
    if (message.pushSent !== false) {
      obj.pushSent = message.pushSent;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<QueueCommandResponse>): QueueCommandResponse {
    return QueueCommandResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueueCommandResponse>): QueueCommandResponse {
    const message = createBaseQueueCommandResponse();
    message.queued = object.queued ?? false;
    message.pushSent = object.pushSent ?? false;
    message.status = object.status ?? "";
    message.error = object.error ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
