// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: common_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "terminal";

/** Terminal session status */
export enum SessionStatus {
  SESSION_STATUS_UNSPECIFIED = 0,
  PENDING = 1,
  CONNECTED = 2,
  /** BACKGROUND - Mobile app in background (added in v2.1.0) */
  BACKGROUND = 3,
  DISCONNECTED = 4,
  ERROR = 5,
  UNRECOGNIZED = -1,
}

export function sessionStatusFromJSON(object: any): SessionStatus {
  switch (object) {
    case 0:
    case "SESSION_STATUS_UNSPECIFIED":
      return SessionStatus.SESSION_STATUS_UNSPECIFIED;
    case 1:
    case "PENDING":
      return SessionStatus.PENDING;
    case 2:
    case "CONNECTED":
      return SessionStatus.CONNECTED;
    case 3:
    case "BACKGROUND":
      return SessionStatus.BACKGROUND;
    case 4:
    case "DISCONNECTED":
      return SessionStatus.DISCONNECTED;
    case 5:
    case "ERROR":
      return SessionStatus.ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionStatus.UNRECOGNIZED;
  }
}

export function sessionStatusToJSON(object: SessionStatus): string {
  switch (object) {
    case SessionStatus.SESSION_STATUS_UNSPECIFIED:
      return "SESSION_STATUS_UNSPECIFIED";
    case SessionStatus.PENDING:
      return "PENDING";
    case SessionStatus.CONNECTED:
      return "CONNECTED";
    case SessionStatus.BACKGROUND:
      return "BACKGROUND";
    case SessionStatus.DISCONNECTED:
      return "DISCONNECTED";
    case SessionStatus.ERROR:
      return "ERROR";
    case SessionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Command execution status */
export enum CommandStatus {
  COMMAND_STATUS_UNSPECIFIED = 0,
  CMD_PENDING = 1,
  CMD_RUNNING = 2,
  CMD_SUCCESS = 3,
  CMD_FAILED = 4,
  CMD_CANCELLED = 5,
  CMD_TIMEOUT = 6,
  UNRECOGNIZED = -1,
}

export function commandStatusFromJSON(object: any): CommandStatus {
  switch (object) {
    case 0:
    case "COMMAND_STATUS_UNSPECIFIED":
      return CommandStatus.COMMAND_STATUS_UNSPECIFIED;
    case 1:
    case "CMD_PENDING":
      return CommandStatus.CMD_PENDING;
    case 2:
    case "CMD_RUNNING":
      return CommandStatus.CMD_RUNNING;
    case 3:
    case "CMD_SUCCESS":
      return CommandStatus.CMD_SUCCESS;
    case 4:
    case "CMD_FAILED":
      return CommandStatus.CMD_FAILED;
    case 5:
    case "CMD_CANCELLED":
      return CommandStatus.CMD_CANCELLED;
    case 6:
    case "CMD_TIMEOUT":
      return CommandStatus.CMD_TIMEOUT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CommandStatus.UNRECOGNIZED;
  }
}

export function commandStatusToJSON(object: CommandStatus): string {
  switch (object) {
    case CommandStatus.COMMAND_STATUS_UNSPECIFIED:
      return "COMMAND_STATUS_UNSPECIFIED";
    case CommandStatus.CMD_PENDING:
      return "CMD_PENDING";
    case CommandStatus.CMD_RUNNING:
      return "CMD_RUNNING";
    case CommandStatus.CMD_SUCCESS:
      return "CMD_SUCCESS";
    case CommandStatus.CMD_FAILED:
      return "CMD_FAILED";
    case CommandStatus.CMD_CANCELLED:
      return "CMD_CANCELLED";
    case CommandStatus.CMD_TIMEOUT:
      return "CMD_TIMEOUT";
    case CommandStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Log levels */
export enum LogLevel {
  LOG_LEVEL_UNSPECIFIED = 0,
  LOG_DEBUG = 1,
  LOG_INFO = 2,
  LOG_WARNING = 3,
  LOG_ERROR = 4,
  UNRECOGNIZED = -1,
}

export function logLevelFromJSON(object: any): LogLevel {
  switch (object) {
    case 0:
    case "LOG_LEVEL_UNSPECIFIED":
      return LogLevel.LOG_LEVEL_UNSPECIFIED;
    case 1:
    case "LOG_DEBUG":
      return LogLevel.LOG_DEBUG;
    case 2:
    case "LOG_INFO":
      return LogLevel.LOG_INFO;
    case 3:
    case "LOG_WARNING":
      return LogLevel.LOG_WARNING;
    case 4:
    case "LOG_ERROR":
      return LogLevel.LOG_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LogLevel.UNRECOGNIZED;
  }
}

export function logLevelToJSON(object: LogLevel): string {
  switch (object) {
    case LogLevel.LOG_LEVEL_UNSPECIFIED:
      return "LOG_LEVEL_UNSPECIFIED";
    case LogLevel.LOG_DEBUG:
      return "LOG_DEBUG";
    case LogLevel.LOG_INFO:
      return "LOG_INFO";
    case LogLevel.LOG_WARNING:
      return "LOG_WARNING";
    case LogLevel.LOG_ERROR:
      return "LOG_ERROR";
    case LogLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Terminal dimensions */
export interface TerminalSize {
  /** Number of rows */
  rows: number;
  /** Number of columns */
  cols: number;
  /** Width in pixels (optional) */
  width: number;
  /** Height in pixels (optional) */
  height: number;
}

/** Session configuration */
export interface SessionConfig {
  /** UUID of the session */
  sessionId: string;
  /** Shell to use (e.g., /bin/zsh) */
  shell: string;
  /** Initial working directory */
  workingDirectory: string;
  /** Environment variables */
  env: { [key: string]: string };
  /** Terminal dimensions */
  size: TerminalSize | undefined;
}

export interface SessionConfig_EnvEntry {
  key: string;
  value: string;
}

/**
 * System metrics collected from the agent machine
 * Sent with HeartbeatUpdate every 30 seconds
 */
export interface SystemMetrics {
  /** Battery information (for laptops/mobile) */
  batteryLevel: number;
  /** Currently charging */
  isCharging: boolean;
  /** Connected to AC power */
  isOnAcPower: boolean;
  /** Resource utilization */
  cpuUsage: number;
  /** 0-100% RAM usage */
  memoryUsage: number;
  /** Total RAM in GB */
  memoryTotalGb: number;
  /** 0-100% root disk usage */
  diskUsage: number;
  /** Total disk in GB */
  diskTotalGb: number;
  /** System information */
  osVersion: string;
  /** Kernel version (empty on iOS) */
  kernelVersion: string;
  /** Primary local IP address */
  ipAddress: string;
  /** Seconds since boot (0 if not available) */
  uptimeSeconds: string;
  /** Number of running processes (0 on mobile) */
  processCount: number;
  /** Architecture info (added in v1.2.0) */
  architecture: string;
}

function createBaseTerminalSize(): TerminalSize {
  return { rows: 0, cols: 0, width: 0, height: 0 };
}

export const TerminalSize: MessageFns<TerminalSize> = {
  encode(message: TerminalSize, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rows !== 0) {
      writer.uint32(8).int32(message.rows);
    }
    if (message.cols !== 0) {
      writer.uint32(16).int32(message.cols);
    }
    if (message.width !== 0) {
      writer.uint32(24).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(32).int32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TerminalSize {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTerminalSize();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rows = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.cols = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TerminalSize {
    return {
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: TerminalSize): unknown {
    const obj: any = {};
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create(base?: DeepPartial<TerminalSize>): TerminalSize {
    return TerminalSize.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TerminalSize>): TerminalSize {
    const message = createBaseTerminalSize();
    message.rows = object.rows ?? 0;
    message.cols = object.cols ?? 0;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseSessionConfig(): SessionConfig {
  return { sessionId: "", shell: "", workingDirectory: "", env: {}, size: undefined };
}

export const SessionConfig: MessageFns<SessionConfig> = {
  encode(message: SessionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.shell !== "") {
      writer.uint32(18).string(message.shell);
    }
    if (message.workingDirectory !== "") {
      writer.uint32(26).string(message.workingDirectory);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      SessionConfig_EnvEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.size !== undefined) {
      TerminalSize.encode(message.size, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.shell = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workingDirectory = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = SessionConfig_EnvEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.env[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.size = TerminalSize.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionConfig {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      shell: isSet(object.shell) ? globalThis.String(object.shell) : "",
      workingDirectory: isSet(object.workingDirectory)
        ? globalThis.String(object.workingDirectory)
        : isSet(object.working_directory)
        ? globalThis.String(object.working_directory)
        : "",
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      size: isSet(object.size) ? TerminalSize.fromJSON(object.size) : undefined,
    };
  },

  toJSON(message: SessionConfig): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.shell !== "") {
      obj.shell = message.shell;
    }
    if (message.workingDirectory !== "") {
      obj.workingDirectory = message.workingDirectory;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.size !== undefined) {
      obj.size = TerminalSize.toJSON(message.size);
    }
    return obj;
  },

  create(base?: DeepPartial<SessionConfig>): SessionConfig {
    return SessionConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionConfig>): SessionConfig {
    const message = createBaseSessionConfig();
    message.sessionId = object.sessionId ?? "";
    message.shell = object.shell ?? "";
    message.workingDirectory = object.workingDirectory ?? "";
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.size = (object.size !== undefined && object.size !== null)
      ? TerminalSize.fromPartial(object.size)
      : undefined;
    return message;
  },
};

function createBaseSessionConfig_EnvEntry(): SessionConfig_EnvEntry {
  return { key: "", value: "" };
}

export const SessionConfig_EnvEntry: MessageFns<SessionConfig_EnvEntry> = {
  encode(message: SessionConfig_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionConfig_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionConfig_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionConfig_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SessionConfig_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<SessionConfig_EnvEntry>): SessionConfig_EnvEntry {
    return SessionConfig_EnvEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionConfig_EnvEntry>): SessionConfig_EnvEntry {
    const message = createBaseSessionConfig_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSystemMetrics(): SystemMetrics {
  return {
    batteryLevel: 0,
    isCharging: false,
    isOnAcPower: false,
    cpuUsage: 0,
    memoryUsage: 0,
    memoryTotalGb: 0,
    diskUsage: 0,
    diskTotalGb: 0,
    osVersion: "",
    kernelVersion: "",
    ipAddress: "",
    uptimeSeconds: "0",
    processCount: 0,
    architecture: "",
  };
}

export const SystemMetrics: MessageFns<SystemMetrics> = {
  encode(message: SystemMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batteryLevel !== 0) {
      writer.uint32(13).float(message.batteryLevel);
    }
    if (message.isCharging !== false) {
      writer.uint32(16).bool(message.isCharging);
    }
    if (message.isOnAcPower !== false) {
      writer.uint32(24).bool(message.isOnAcPower);
    }
    if (message.cpuUsage !== 0) {
      writer.uint32(37).float(message.cpuUsage);
    }
    if (message.memoryUsage !== 0) {
      writer.uint32(45).float(message.memoryUsage);
    }
    if (message.memoryTotalGb !== 0) {
      writer.uint32(53).float(message.memoryTotalGb);
    }
    if (message.diskUsage !== 0) {
      writer.uint32(61).float(message.diskUsage);
    }
    if (message.diskTotalGb !== 0) {
      writer.uint32(69).float(message.diskTotalGb);
    }
    if (message.osVersion !== "") {
      writer.uint32(74).string(message.osVersion);
    }
    if (message.kernelVersion !== "") {
      writer.uint32(82).string(message.kernelVersion);
    }
    if (message.ipAddress !== "") {
      writer.uint32(90).string(message.ipAddress);
    }
    if (message.uptimeSeconds !== "0") {
      writer.uint32(96).int64(message.uptimeSeconds);
    }
    if (message.processCount !== 0) {
      writer.uint32(104).int32(message.processCount);
    }
    if (message.architecture !== "") {
      writer.uint32(114).string(message.architecture);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SystemMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.batteryLevel = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isCharging = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isOnAcPower = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.cpuUsage = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.memoryUsage = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.memoryTotalGb = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.diskUsage = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.diskTotalGb = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.osVersion = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.kernelVersion = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.ipAddress = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.uptimeSeconds = reader.int64().toString();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.processCount = reader.int32();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.architecture = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SystemMetrics {
    return {
      batteryLevel: isSet(object.batteryLevel)
        ? globalThis.Number(object.batteryLevel)
        : isSet(object.battery_level)
        ? globalThis.Number(object.battery_level)
        : 0,
      isCharging: isSet(object.isCharging)
        ? globalThis.Boolean(object.isCharging)
        : isSet(object.is_charging)
        ? globalThis.Boolean(object.is_charging)
        : false,
      isOnAcPower: isSet(object.isOnAcPower)
        ? globalThis.Boolean(object.isOnAcPower)
        : isSet(object.is_on_ac_power)
        ? globalThis.Boolean(object.is_on_ac_power)
        : false,
      cpuUsage: isSet(object.cpuUsage)
        ? globalThis.Number(object.cpuUsage)
        : isSet(object.cpu_usage)
        ? globalThis.Number(object.cpu_usage)
        : 0,
      memoryUsage: isSet(object.memoryUsage)
        ? globalThis.Number(object.memoryUsage)
        : isSet(object.memory_usage)
        ? globalThis.Number(object.memory_usage)
        : 0,
      memoryTotalGb: isSet(object.memoryTotalGb)
        ? globalThis.Number(object.memoryTotalGb)
        : isSet(object.memory_total_gb)
        ? globalThis.Number(object.memory_total_gb)
        : 0,
      diskUsage: isSet(object.diskUsage)
        ? globalThis.Number(object.diskUsage)
        : isSet(object.disk_usage)
        ? globalThis.Number(object.disk_usage)
        : 0,
      diskTotalGb: isSet(object.diskTotalGb)
        ? globalThis.Number(object.diskTotalGb)
        : isSet(object.disk_total_gb)
        ? globalThis.Number(object.disk_total_gb)
        : 0,
      osVersion: isSet(object.osVersion)
        ? globalThis.String(object.osVersion)
        : isSet(object.os_version)
        ? globalThis.String(object.os_version)
        : "",
      kernelVersion: isSet(object.kernelVersion)
        ? globalThis.String(object.kernelVersion)
        : isSet(object.kernel_version)
        ? globalThis.String(object.kernel_version)
        : "",
      ipAddress: isSet(object.ipAddress)
        ? globalThis.String(object.ipAddress)
        : isSet(object.ip_address)
        ? globalThis.String(object.ip_address)
        : "",
      uptimeSeconds: isSet(object.uptimeSeconds)
        ? globalThis.String(object.uptimeSeconds)
        : isSet(object.uptime_seconds)
        ? globalThis.String(object.uptime_seconds)
        : "0",
      processCount: isSet(object.processCount)
        ? globalThis.Number(object.processCount)
        : isSet(object.process_count)
        ? globalThis.Number(object.process_count)
        : 0,
      architecture: isSet(object.architecture) ? globalThis.String(object.architecture) : "",
    };
  },

  toJSON(message: SystemMetrics): unknown {
    const obj: any = {};
    if (message.batteryLevel !== 0) {
      obj.batteryLevel = message.batteryLevel;
    }
    if (message.isCharging !== false) {
      obj.isCharging = message.isCharging;
    }
    if (message.isOnAcPower !== false) {
      obj.isOnAcPower = message.isOnAcPower;
    }
    if (message.cpuUsage !== 0) {
      obj.cpuUsage = message.cpuUsage;
    }
    if (message.memoryUsage !== 0) {
      obj.memoryUsage = message.memoryUsage;
    }
    if (message.memoryTotalGb !== 0) {
      obj.memoryTotalGb = message.memoryTotalGb;
    }
    if (message.diskUsage !== 0) {
      obj.diskUsage = message.diskUsage;
    }
    if (message.diskTotalGb !== 0) {
      obj.diskTotalGb = message.diskTotalGb;
    }
    if (message.osVersion !== "") {
      obj.osVersion = message.osVersion;
    }
    if (message.kernelVersion !== "") {
      obj.kernelVersion = message.kernelVersion;
    }
    if (message.ipAddress !== "") {
      obj.ipAddress = message.ipAddress;
    }
    if (message.uptimeSeconds !== "0") {
      obj.uptimeSeconds = message.uptimeSeconds;
    }
    if (message.processCount !== 0) {
      obj.processCount = Math.round(message.processCount);
    }
    if (message.architecture !== "") {
      obj.architecture = message.architecture;
    }
    return obj;
  },

  create(base?: DeepPartial<SystemMetrics>): SystemMetrics {
    return SystemMetrics.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SystemMetrics>): SystemMetrics {
    const message = createBaseSystemMetrics();
    message.batteryLevel = object.batteryLevel ?? 0;
    message.isCharging = object.isCharging ?? false;
    message.isOnAcPower = object.isOnAcPower ?? false;
    message.cpuUsage = object.cpuUsage ?? 0;
    message.memoryUsage = object.memoryUsage ?? 0;
    message.memoryTotalGb = object.memoryTotalGb ?? 0;
    message.diskUsage = object.diskUsage ?? 0;
    message.diskTotalGb = object.diskTotalGb ?? 0;
    message.osVersion = object.osVersion ?? "";
    message.kernelVersion = object.kernelVersion ?? "";
    message.ipAddress = object.ipAddress ?? "";
    message.uptimeSeconds = object.uptimeSeconds ?? "0";
    message.processCount = object.processCount ?? 0;
    message.architecture = object.architecture ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
