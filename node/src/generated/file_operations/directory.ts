// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: file_operations/directory.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FileVisibilityMode, fileVisibilityModeFromJSON, fileVisibilityModeToJSON, StreamFileEntry } from "./common";

export const protobufPackage = "terminal";

export interface FileListDirectoryRequest {
  path: string;
  /** Default: 100 */
  pageSize: number;
  pageToken: string;
  /** DEPRECATED: Use visibility_mode instead */
  includeHidden: boolean;
  /** Smart visibility control (v2.10.0) */
  visibilityMode: FileVisibilityMode;
}

export interface FileListDirectoryResult {
  currentPath: string;
  entries: StreamFileEntry[];
  nextPageToken: string;
  totalCount: number;
  hasMore: boolean;
}

export interface FileCreateDirectoryRequest {
  path: string;
  /** mkdir -p */
  createParents: boolean;
}

export interface FileCreateDirectoryResult {
  entry: StreamFileEntry | undefined;
}

function createBaseFileListDirectoryRequest(): FileListDirectoryRequest {
  return { path: "", pageSize: 0, pageToken: "", includeHidden: false, visibilityMode: 0 };
}

export const FileListDirectoryRequest: MessageFns<FileListDirectoryRequest> = {
  encode(message: FileListDirectoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.pageSize !== 0) {
      writer.uint32(16).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(26).string(message.pageToken);
    }
    if (message.includeHidden !== false) {
      writer.uint32(32).bool(message.includeHidden);
    }
    if (message.visibilityMode !== 0) {
      writer.uint32(40).int32(message.visibilityMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileListDirectoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileListDirectoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.includeHidden = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.visibilityMode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileListDirectoryRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      pageSize: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      pageToken: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : "",
      includeHidden: isSet(object.includeHidden)
        ? globalThis.Boolean(object.includeHidden)
        : isSet(object.include_hidden)
        ? globalThis.Boolean(object.include_hidden)
        : false,
      visibilityMode: isSet(object.visibilityMode)
        ? fileVisibilityModeFromJSON(object.visibilityMode)
        : isSet(object.visibility_mode)
        ? fileVisibilityModeFromJSON(object.visibility_mode)
        : 0,
    };
  },

  toJSON(message: FileListDirectoryRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    if (message.includeHidden !== false) {
      obj.includeHidden = message.includeHidden;
    }
    if (message.visibilityMode !== 0) {
      obj.visibilityMode = fileVisibilityModeToJSON(message.visibilityMode);
    }
    return obj;
  },

  create(base?: DeepPartial<FileListDirectoryRequest>): FileListDirectoryRequest {
    return FileListDirectoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileListDirectoryRequest>): FileListDirectoryRequest {
    const message = createBaseFileListDirectoryRequest();
    message.path = object.path ?? "";
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    message.includeHidden = object.includeHidden ?? false;
    message.visibilityMode = object.visibilityMode ?? 0;
    return message;
  },
};

function createBaseFileListDirectoryResult(): FileListDirectoryResult {
  return { currentPath: "", entries: [], nextPageToken: "", totalCount: 0, hasMore: false };
}

export const FileListDirectoryResult: MessageFns<FileListDirectoryResult> = {
  encode(message: FileListDirectoryResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentPath !== "") {
      writer.uint32(10).string(message.currentPath);
    }
    for (const v of message.entries) {
      StreamFileEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(26).string(message.nextPageToken);
    }
    if (message.totalCount !== 0) {
      writer.uint32(32).int32(message.totalCount);
    }
    if (message.hasMore !== false) {
      writer.uint32(40).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileListDirectoryResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileListDirectoryResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.currentPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entries.push(StreamFileEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileListDirectoryResult {
    return {
      currentPath: isSet(object.currentPath)
        ? globalThis.String(object.currentPath)
        : isSet(object.current_path)
        ? globalThis.String(object.current_path)
        : "",
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => StreamFileEntry.fromJSON(e))
        : [],
      nextPageToken: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : "",
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
      hasMore: isSet(object.hasMore)
        ? globalThis.Boolean(object.hasMore)
        : isSet(object.has_more)
        ? globalThis.Boolean(object.has_more)
        : false,
    };
  },

  toJSON(message: FileListDirectoryResult): unknown {
    const obj: any = {};
    if (message.currentPath !== "") {
      obj.currentPath = message.currentPath;
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => StreamFileEntry.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.hasMore !== false) {
      obj.hasMore = message.hasMore;
    }
    return obj;
  },

  create(base?: DeepPartial<FileListDirectoryResult>): FileListDirectoryResult {
    return FileListDirectoryResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileListDirectoryResult>): FileListDirectoryResult {
    const message = createBaseFileListDirectoryResult();
    message.currentPath = object.currentPath ?? "";
    message.entries = object.entries?.map((e) => StreamFileEntry.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    message.totalCount = object.totalCount ?? 0;
    message.hasMore = object.hasMore ?? false;
    return message;
  },
};

function createBaseFileCreateDirectoryRequest(): FileCreateDirectoryRequest {
  return { path: "", createParents: false };
}

export const FileCreateDirectoryRequest: MessageFns<FileCreateDirectoryRequest> = {
  encode(message: FileCreateDirectoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.createParents !== false) {
      writer.uint32(16).bool(message.createParents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateDirectoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateDirectoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.createParents = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateDirectoryRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      createParents: isSet(object.createParents)
        ? globalThis.Boolean(object.createParents)
        : isSet(object.create_parents)
        ? globalThis.Boolean(object.create_parents)
        : false,
    };
  },

  toJSON(message: FileCreateDirectoryRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.createParents !== false) {
      obj.createParents = message.createParents;
    }
    return obj;
  },

  create(base?: DeepPartial<FileCreateDirectoryRequest>): FileCreateDirectoryRequest {
    return FileCreateDirectoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileCreateDirectoryRequest>): FileCreateDirectoryRequest {
    const message = createBaseFileCreateDirectoryRequest();
    message.path = object.path ?? "";
    message.createParents = object.createParents ?? false;
    return message;
  },
};

function createBaseFileCreateDirectoryResult(): FileCreateDirectoryResult {
  return { entry: undefined };
}

export const FileCreateDirectoryResult: MessageFns<FileCreateDirectoryResult> = {
  encode(message: FileCreateDirectoryResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entry !== undefined) {
      StreamFileEntry.encode(message.entry, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileCreateDirectoryResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileCreateDirectoryResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entry = StreamFileEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileCreateDirectoryResult {
    return { entry: isSet(object.entry) ? StreamFileEntry.fromJSON(object.entry) : undefined };
  },

  toJSON(message: FileCreateDirectoryResult): unknown {
    const obj: any = {};
    if (message.entry !== undefined) {
      obj.entry = StreamFileEntry.toJSON(message.entry);
    }
    return obj;
  },

  create(base?: DeepPartial<FileCreateDirectoryResult>): FileCreateDirectoryResult {
    return FileCreateDirectoryResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileCreateDirectoryResult>): FileCreateDirectoryResult {
    const message = createBaseFileCreateDirectoryResult();
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? StreamFileEntry.fromPartial(object.entry)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
