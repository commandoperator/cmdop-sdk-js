// Auto-generated by DjangoCFG - see CLAUDE.md
/**
 * Typed fetchers for Machine Sharing
 *
 * Universal functions that work in any environment:
 * - Next.js (App Router / Pages Router / Server Components)
 * - React Native
 * - Node.js backend
 *
 * These fetchers use Zod schemas for runtime validation.
 *
 * Usage:
 * ```typescript
 * // Configure API once (in your app entry point)
 * import { configureAPI } from '../../api-instance'
 * configureAPI({ baseUrl: 'https://api.example.com' })
 *
 * // Then use fetchers anywhere
 * const users = await getUsers({ page: 1 })
 *
 * // With SWR
 * const { data } = useSWR(['users', params], () => getUsers(params))
 *
 * // With React Query
 * const { data } = useQuery(['users', params], () => getUsers(params))
 *
 * // In Server Component or SSR (pass custom client)
 * import { API } from '../../index'
 * const api = new API('https://api.example.com')
 * const users = await getUsers({ page: 1 }, api)
 * ```
 */
import { consola } from 'consola'
import { PaginatedSharedMachineListListSchema, type PaginatedSharedMachineListList } from '../schemas/PaginatedSharedMachineListList.schema'
import { SharedMachineSchema, type SharedMachine } from '../schemas/SharedMachine.schema'
import { SharedMachineCreateRequestSchema, type SharedMachineCreateRequest } from '../schemas/SharedMachineCreateRequest.schema'
import { getAPIInstance } from '../../api-instance'

/**
 * Create share link for machine
 *
 * @method POST
 * @path /api/machines/machines/{id}/share/
 */
export async function createMachinesMachinesShareCreate(  id: string, data: SharedMachineCreateRequest,  client?: any
): Promise<SharedMachine> {
  const api = client || getAPIInstance()
  const response = await api.machines_machine_sharing.machinesMachinesShareCreate(id, data)
  try {
    return SharedMachineSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createMachinesMachinesShareCreate\nPath: /api/machines/machines/{id}/share/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createMachinesMachinesShareCreate',
            path: '/api/machines/machines/{id}/share/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * List active shares for machine
 *
 * @method GET
 * @path /api/machines/machines/{id}/shares/
 */
export async function getMachinesMachinesSharesList(  id: string, params?: { ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedSharedMachineListList> {
  const api = client || getAPIInstance()
  const response = await api.machines_machine_sharing.machinesMachinesSharesList(id, params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedSharedMachineListListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesMachinesSharesList\nPath: /api/machines/machines/{id}/shares/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesMachinesSharesList',
            path: '/api/machines/machines/{id}/shares/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Remove all shares for machine
 *
 * @method DELETE
 * @path /api/machines/machines/{id}/unshare/
 */
export async function deleteMachinesMachinesUnshareDestroy(  id: string,  client?: any
): Promise<void> {
  const api = client || getAPIInstance()
  const response = await api.machines_machine_sharing.machinesMachinesUnshareDestroy(id)
  return response
}


