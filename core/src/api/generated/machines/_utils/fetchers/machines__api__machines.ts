// Auto-generated by DjangoCFG - see CLAUDE.md
/**
 * Typed fetchers for Machines
 *
 * Universal functions that work in any environment:
 * - Next.js (App Router / Pages Router / Server Components)
 * - React Native
 * - Node.js backend
 *
 * These fetchers use Zod schemas for runtime validation.
 *
 * Usage:
 * ```typescript
 * // Configure API once (in your app entry point)
 * import { configureAPI } from '../../api-instance'
 * configureAPI({ baseUrl: 'https://api.example.com' })
 *
 * // Then use fetchers anywhere
 * const users = await getUsers({ page: 1 })
 *
 * // With SWR
 * const { data } = useSWR(['users', params], () => getUsers(params))
 *
 * // With React Query
 * const { data } = useQuery(['users', params], () => getUsers(params))
 *
 * // In Server Component or SSR (pass custom client)
 * import { API } from '../../index'
 * const api = new API('https://api.example.com')
 * const users = await getUsers({ page: 1 }, api)
 * ```
 */
import { consola } from 'consola'
import { MachineSchema, type Machine } from '../schemas/Machine.schema'
import { MachineCreateSchema, type MachineCreate } from '../schemas/MachineCreate.schema'
import { MachineCreateRequestSchema, type MachineCreateRequest } from '../schemas/MachineCreateRequest.schema'
import { MachineLogSchema, type MachineLog } from '../schemas/MachineLog.schema'
import { MachineLogRequestSchema, type MachineLogRequest } from '../schemas/MachineLogRequest.schema'
import { MachineRequestSchema, type MachineRequest } from '../schemas/MachineRequest.schema'
import { MachinesMachinesUpdateMetricsCreateRequestSchema, type MachinesMachinesUpdateMetricsCreateRequest } from '../schemas/MachinesMachinesUpdateMetricsCreateRequest.schema'
import { PaginatedMachineListSchema, type PaginatedMachineList } from '../schemas/PaginatedMachineList.schema'
import { PaginatedMachineLogListSchema, type PaginatedMachineLogList } from '../schemas/PaginatedMachineLogList.schema'
import { PatchedMachineRequestSchema, type PatchedMachineRequest } from '../schemas/PatchedMachineRequest.schema'
import { getAPIInstance } from '../../api-instance'

/**
 * API operation
 *
 * @method GET
 * @path /api/machines/logs/
 */
export async function getMachinesLogsList(  params?: { ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedMachineLogList> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.logsList(params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedMachineLogListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesLogsList\nPath: /api/machines/logs/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesLogsList',
            path: '/api/machines/logs/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method POST
 * @path /api/machines/logs/
 */
export async function createMachinesLogsCreate(  data: MachineLogRequest,  client?: any
): Promise<MachineLog> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.logsCreate(data)
  try {
    return MachineLogSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createMachinesLogsCreate\nPath: /api/machines/logs/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createMachinesLogsCreate',
            path: '/api/machines/logs/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/machines/logs/{id}/
 */
export async function getMachinesLogsRetrieve(  id: string,  client?: any
): Promise<MachineLog> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.logsRetrieve(id)
  try {
    return MachineLogSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesLogsRetrieve\nPath: /api/machines/logs/{id}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesLogsRetrieve',
            path: '/api/machines/logs/{id}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/machines/machines/
 */
export async function getMachinesMachinesList(  params?: { ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedMachineList> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesList(params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedMachineListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesMachinesList\nPath: /api/machines/machines/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesMachinesList',
            path: '/api/machines/machines/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method POST
 * @path /api/machines/machines/
 */
export async function createMachinesMachinesCreate(  data: MachineCreateRequest,  client?: any
): Promise<MachineCreate> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesCreate(data)
  try {
    return MachineCreateSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createMachinesMachinesCreate\nPath: /api/machines/machines/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createMachinesMachinesCreate',
            path: '/api/machines/machines/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/machines/machines/{id}/
 */
export async function getMachinesMachinesRetrieve(  id: string,  client?: any
): Promise<Machine> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesRetrieve(id)
  try {
    return MachineSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesMachinesRetrieve\nPath: /api/machines/machines/{id}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesMachinesRetrieve',
            path: '/api/machines/machines/{id}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PUT
 * @path /api/machines/machines/{id}/
 */
export async function updateMachinesMachinesUpdate(  id: string, data: MachineRequest,  client?: any
): Promise<Machine> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesUpdate(id, data)
  try {
    return MachineSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`updateMachinesMachinesUpdate\nPath: /api/machines/machines/{id}/\nMethod: PUT`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'updateMachinesMachinesUpdate',
            path: '/api/machines/machines/{id}/',
            method: 'PUT',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PATCH
 * @path /api/machines/machines/{id}/
 */
export async function partialUpdateMachinesMachinesPartialUpdate(  id: string, data?: PatchedMachineRequest,  client?: any
): Promise<Machine> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesPartialUpdate(id, data)
  try {
    return MachineSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`partialUpdateMachinesMachinesPartialUpdate\nPath: /api/machines/machines/{id}/\nMethod: PATCH`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'partialUpdateMachinesMachinesPartialUpdate',
            path: '/api/machines/machines/{id}/',
            method: 'PATCH',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method DELETE
 * @path /api/machines/machines/{id}/
 */
export async function deleteMachinesMachinesDestroy(  id: string,  client?: any
): Promise<void> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesDestroy(id)
  return response
}


/**
 * Get machine logs
 *
 * @method GET
 * @path /api/machines/machines/{id}/logs/
 */
export async function getMachinesMachinesLogsList(  id: string, params?: { level?: string; limit?: number; ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedMachineLogList> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesLogsList(id, params?.level, params?.limit, params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedMachineLogListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getMachinesMachinesLogsList\nPath: /api/machines/machines/{id}/logs/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getMachinesMachinesLogsList',
            path: '/api/machines/machines/{id}/logs/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Regenerate agent token
 *
 * @method POST
 * @path /api/machines/machines/{id}/regenerate-token/
 */
export async function createMachinesMachinesRegenerateTokenCreate(  id: string, data: MachineRequest,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesRegenerateTokenCreate(id, data)
  return response
}


/**
 * Get machine statistics
 *
 * @method GET
 * @path /api/machines/machines/{id}/stats/
 */
export async function getMachinesMachinesStatsRetrieve(  id: string,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesStatsRetrieve(id)
  return response
}


/**
 * Update machine metrics
 *
 * @method POST
 * @path /api/machines/machines/{id}/update-metrics/
 */
export async function createMachinesMachinesUpdateMetricsCreate(  id: string, data: MachinesMachinesUpdateMetricsCreateRequest,  client?: any
): Promise<Machine> {
  const api = client || getAPIInstance()
  const response = await api.machines_machines.machinesUpdateMetricsCreate(id, data)
  try {
    return MachineSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createMachinesMachinesUpdateMetricsCreate\nPath: /api/machines/machines/{id}/update-metrics/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createMachinesMachinesUpdateMetricsCreate',
            path: '/api/machines/machines/{id}/update-metrics/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


