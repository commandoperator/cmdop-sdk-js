// @ts-nocheck
// Auto-generated by DjangoCFG - see CLAUDE.md
import * as Enums from "../enums";

/**
 * Serializer for creating a new share.
 * 
 * Request model (no read-only fields).
 */
export interface SharedMachineCreateRequest {
  /** Hours until share expires (null = never) */
  expires_in_hours?: number | null;
}

/**
 * Full shared machine details (for owners).
 * 
 * Response model (includes read-only fields).
 */
export interface SharedMachine {
  id: string;
  /** Unique token for public access */
  share_token: string;
  /** Generate full share URL. */
  share_url: string;
  /** * `read_only` - Read Only */
  permission?: Enums.SharedMachinePermission;
  machine: string;
  machine_name: string;
  machine_hostname: string;
  machine_status: string;
  /** When this share expires (null = never) */
  expires_at?: string | null;
  views_count: number;
  last_viewed_at?: string | null;
  /** Can be deactivated without deleting */
  is_active?: boolean;
  is_expired: boolean;
  is_valid: boolean;
  /** Count active terminal sessions on this machine. */
  active_sessions_count: number;
  created_by: number;
  created_at: string;
}

/**
 * 
 * Response model (includes read-only fields).
 */
export interface PaginatedSharedMachineListList {
  /** Total number of items across all pages */
  count: number;
  /** Current page number (1-based) */
  page: number;
  /** Total number of pages */
  pages: number;
  /** Number of items per page */
  page_size: number;
  /** Whether there is a next page */
  has_next: boolean;
  /** Whether there is a previous page */
  has_previous: boolean;
  /** Next page number (null if no next page) */
  next_page?: number | null;
  /** Previous page number (null if no previous page) */
  previous_page?: number | null;
  /** Array of items for current page */
  results: Array<SharedMachineList>;
}

/**
 * Lightweight serializer for listing shares.
 * 
 * Response model (includes read-only fields).
 */
export interface SharedMachineList {
  id: string;
  /** Unique token for public access */
  share_token: string;
  /** * `read_only` - Read Only */
  permission?: Enums.SharedMachinePermission;
  machine: string;
  machine_name: string;
  /** When this share expires (null = never) */
  expires_at?: string | null;
  views_count?: number;
  /** Can be deactivated without deleting */
  is_active?: boolean;
  is_expired: boolean;
  is_valid: boolean;
  created_at: string;
}

