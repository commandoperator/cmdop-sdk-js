// @ts-nocheck
// Auto-generated by DjangoCFG - see CLAUDE.md
/**
 * Typed fetchers for Oauth
 *
 * Universal functions that work in any environment:
 * - Next.js (App Router / Pages Router / Server Components)
 * - React Native
 * - Node.js backend
 *
 * These fetchers use Zod schemas for runtime validation.
 *
 * Usage:
 * ```typescript
 * // Configure API once (in your app entry point)
 * import { configureAPI } from '../../api-instance'
 * configureAPI({ baseUrl: 'https://api.example.com' })
 *
 * // Then use fetchers anywhere
 * const users = await getUsers({ page: 1 })
 *
 * // With SWR
 * const { data } = useSWR(['users', params], () => getUsers(params))
 *
 * // With React Query
 * const { data } = useQuery(['users', params], () => getUsers(params))
 *
 * // In Server Component or SSR (pass custom client)
 * import { API } from '../../index'
 * const api = new API('https://api.example.com')
 * const users = await getUsers({ page: 1 }, api)
 * ```
 */
import { consola } from 'consola'
import { DeviceAuthorizeRequestSchema, type DeviceAuthorizeRequest } from '../schemas/DeviceAuthorizeRequest.schema'
import { DeviceAuthorizeResponseSchema, type DeviceAuthorizeResponse } from '../schemas/DeviceAuthorizeResponse.schema'
import { DeviceCodeRequestRequestSchema, type DeviceCodeRequestRequest } from '../schemas/DeviceCodeRequestRequest.schema'
import { DeviceCodeResponseSchema, type DeviceCodeResponse } from '../schemas/DeviceCodeResponse.schema'
import { PaginatedTokenListListSchema, type PaginatedTokenListList } from '../schemas/PaginatedTokenListList.schema'
import { TokenInfoSchema, type TokenInfo } from '../schemas/TokenInfo.schema'
import { TokenRequestRequestSchema, type TokenRequestRequest } from '../schemas/TokenRequestRequest.schema'
import { TokenResponseSchema, type TokenResponse } from '../schemas/TokenResponse.schema'
import { TokenRevokeRequestSchema, type TokenRevokeRequest } from '../schemas/TokenRevokeRequest.schema'
import { getAPIInstance } from '../../api-instance'

/**
 * Authorize device
 *
 * @method POST
 * @path /api/system/oauth/authorize/
 */
export async function createSystemOauthAuthorizeCreate(  data: DeviceAuthorizeRequest,  client?: any
): Promise<DeviceAuthorizeResponse> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthAuthorizeCreate(data)
  try {
    return DeviceAuthorizeResponseSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createSystemOauthAuthorizeCreate\nPath: /api/system/oauth/authorize/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createSystemOauthAuthorizeCreate',
            path: '/api/system/oauth/authorize/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Request device code
 *
 * @method POST
 * @path /api/system/oauth/device/
 */
export async function createSystemOauthDeviceCreate(  data: DeviceCodeRequestRequest,  client?: any
): Promise<DeviceCodeResponse> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthDeviceCreate(data)
  try {
    return DeviceCodeResponseSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createSystemOauthDeviceCreate\nPath: /api/system/oauth/device/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createSystemOauthDeviceCreate',
            path: '/api/system/oauth/device/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Revoke token
 *
 * @method POST
 * @path /api/system/oauth/revoke/
 */
export async function createSystemOauthRevokeCreate(  data: TokenRevokeRequest,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthRevokeCreate(data)
  return response
}


/**
 * Request access token
 *
 * @method POST
 * @path /api/system/oauth/token/
 */
export async function createSystemOauthTokenCreate(  data: TokenRequestRequest,  client?: any
): Promise<TokenResponse> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthTokenCreate(data)
  try {
    return TokenResponseSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createSystemOauthTokenCreate\nPath: /api/system/oauth/token/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createSystemOauthTokenCreate',
            path: '/api/system/oauth/token/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Get token info
 *
 * @method GET
 * @path /api/system/oauth/token/info/
 */
export async function getSystemOauthTokenInfoRetrieve(  client?: any
): Promise<TokenInfo> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthTokenInfoRetrieve()
  try {
    return TokenInfoSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getSystemOauthTokenInfoRetrieve\nPath: /api/system/oauth/token/info/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getSystemOauthTokenInfoRetrieve',
            path: '/api/system/oauth/token/info/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * List user tokens
 *
 * @method GET
 * @path /api/system/oauth/tokens/
 */
export async function getSystemOauthTokensList(  params?: { ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedTokenListList> {
  const api = client || getAPIInstance()
  const response = await api.system_oauth.systemOauthTokensList(params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedTokenListListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getSystemOauthTokensList\nPath: /api/system/oauth/tokens/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getSystemOauthTokensList',
            path: '/api/system/oauth/tokens/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


