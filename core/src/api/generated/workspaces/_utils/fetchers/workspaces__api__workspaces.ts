// @ts-nocheck
// Auto-generated by DjangoCFG - see CLAUDE.md
/**
 * Typed fetchers for Workspaces
 *
 * Universal functions that work in any environment:
 * - Next.js (App Router / Pages Router / Server Components)
 * - React Native
 * - Node.js backend
 *
 * These fetchers use Zod schemas for runtime validation.
 *
 * Usage:
 * ```typescript
 * // Configure API once (in your app entry point)
 * import { configureAPI } from '../../api-instance'
 * configureAPI({ baseUrl: 'https://api.example.com' })
 *
 * // Then use fetchers anywhere
 * const users = await getUsers({ page: 1 })
 *
 * // With SWR
 * const { data } = useSWR(['users', params], () => getUsers(params))
 *
 * // With React Query
 * const { data } = useQuery(['users', params], () => getUsers(params))
 *
 * // In Server Component or SSR (pass custom client)
 * import { API } from '../../index'
 * const api = new API('https://api.example.com')
 * const users = await getUsers({ page: 1 }, api)
 * ```
 */
import { consola } from 'consola'
import { PaginatedWorkspaceInvitationListSchema, type PaginatedWorkspaceInvitationList } from '../schemas/PaginatedWorkspaceInvitationList.schema'
import { PaginatedWorkspaceMemberListSchema, type PaginatedWorkspaceMemberList } from '../schemas/PaginatedWorkspaceMemberList.schema'
import { PatchedWorkspaceMemberRequestSchema, type PatchedWorkspaceMemberRequest } from '../schemas/PatchedWorkspaceMemberRequest.schema'
import { PatchedWorkspaceRequestSchema, type PatchedWorkspaceRequest } from '../schemas/PatchedWorkspaceRequest.schema'
import { WorkspaceSchema, type Workspace } from '../schemas/Workspace.schema'
import { WorkspaceCreateRequestSchema, type WorkspaceCreateRequest } from '../schemas/WorkspaceCreateRequest.schema'
import { WorkspaceInvitationSchema, type WorkspaceInvitation } from '../schemas/WorkspaceInvitation.schema'
import { WorkspaceInvitationAcceptRequestSchema, type WorkspaceInvitationAcceptRequest } from '../schemas/WorkspaceInvitationAcceptRequest.schema'
import { WorkspaceInvitationCreateRequestSchema, type WorkspaceInvitationCreateRequest } from '../schemas/WorkspaceInvitationCreateRequest.schema'
import { WorkspaceInvitationPublicSchema, type WorkspaceInvitationPublic } from '../schemas/WorkspaceInvitationPublic.schema'
import { WorkspaceInvitationRequestSchema, type WorkspaceInvitationRequest } from '../schemas/WorkspaceInvitationRequest.schema'
import { WorkspaceMemberSchema, type WorkspaceMember } from '../schemas/WorkspaceMember.schema'
import { WorkspaceMemberRequestSchema, type WorkspaceMemberRequest } from '../schemas/WorkspaceMemberRequest.schema'
import { WorkspaceRequestSchema, type WorkspaceRequest } from '../schemas/WorkspaceRequest.schema'
import { getAPIInstance } from '../../api-instance'

/**
 * List invitations
 *
 * @method GET
 * @path /api/workspaces/invitations/
 */
export async function getWorkspacesInvitationsList(  params?: { ordering?: string; page?: number; page_size?: number; search?: string },  client?: any
): Promise<PaginatedWorkspaceInvitationList> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsList(params?.ordering, params?.page, params?.page_size, params?.search)
  try {
    return PaginatedWorkspaceInvitationListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesInvitationsList\nPath: /api/workspaces/invitations/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesInvitationsList',
            path: '/api/workspaces/invitations/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Create invitation
 *
 * @method POST
 * @path /api/workspaces/invitations/
 */
export async function createWorkspacesInvitationsCreate(  data: WorkspaceInvitationCreateRequest,  client?: any
): Promise<WorkspaceInvitation> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsCreate(data)
  try {
    return WorkspaceInvitationSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createWorkspacesInvitationsCreate\nPath: /api/workspaces/invitations/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createWorkspacesInvitationsCreate',
            path: '/api/workspaces/invitations/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Get invitation details
 *
 * @method GET
 * @path /api/workspaces/invitations/{id}/
 */
export async function getWorkspacesInvitationsRetrieve(  id: string,  client?: any
): Promise<WorkspaceInvitation> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsRetrieve(id)
  try {
    return WorkspaceInvitationSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesInvitationsRetrieve\nPath: /api/workspaces/invitations/{id}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesInvitationsRetrieve',
            path: '/api/workspaces/invitations/{id}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Cancel invitation
 *
 * @method DELETE
 * @path /api/workspaces/invitations/{id}/
 */
export async function deleteWorkspacesInvitationsDestroy(  id: string,  client?: any
): Promise<void> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsDestroy(id)
  return response
}


/**
 * Resend invitation
 *
 * @method POST
 * @path /api/workspaces/invitations/{id}/resend/
 */
export async function createWorkspacesInvitationsResendCreate(  id: string, data: WorkspaceInvitationRequest,  client?: any
): Promise<WorkspaceInvitation> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsResendCreate(id, data)
  try {
    return WorkspaceInvitationSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createWorkspacesInvitationsResendCreate\nPath: /api/workspaces/invitations/{id}/resend/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createWorkspacesInvitationsResendCreate',
            path: '/api/workspaces/invitations/{id}/resend/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * Accept invitation
 *
 * @method POST
 * @path /api/workspaces/invitations/accept/
 */
export async function createWorkspacesInvitationsAcceptCreate(  data: WorkspaceInvitationAcceptRequest,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsAcceptCreate(data)
  return response
}


/**
 * Decline invitation
 *
 * @method POST
 * @path /api/workspaces/invitations/decline/
 */
export async function createWorkspacesInvitationsDeclineCreate(  data: WorkspaceInvitationAcceptRequest,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsDeclineCreate(data)
  return response
}


/**
 * Get invitation details by token
 *
 * @method GET
 * @path /api/workspaces/invitations/details/{token}/
 */
export async function getWorkspacesInvitationsDetailsRetrieve(  token: string,  client?: any
): Promise<WorkspaceInvitationPublic> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.invitationsDetailsRetrieve(token)
  try {
    return WorkspaceInvitationPublicSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesInvitationsDetailsRetrieve\nPath: /api/workspaces/invitations/details/{token}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesInvitationsDetailsRetrieve',
            path: '/api/workspaces/invitations/details/{token}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/workspaces/members/
 */
export async function getWorkspacesMembersList(  params?: { ordering?: string; page?: number; page_size?: number; role?: string; search?: string },  client?: any
): Promise<PaginatedWorkspaceMemberList> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersList(params?.ordering, params?.page, params?.page_size, params?.role, params?.search)
  try {
    return PaginatedWorkspaceMemberListSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesMembersList\nPath: /api/workspaces/members/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesMembersList',
            path: '/api/workspaces/members/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method POST
 * @path /api/workspaces/members/
 */
export async function createWorkspacesMembersCreate(  data: WorkspaceMemberRequest,  client?: any
): Promise<WorkspaceMember> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersCreate(data)
  try {
    return WorkspaceMemberSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createWorkspacesMembersCreate\nPath: /api/workspaces/members/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createWorkspacesMembersCreate',
            path: '/api/workspaces/members/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/workspaces/members/{id}/
 */
export async function getWorkspacesMembersRetrieve(  id: string,  client?: any
): Promise<WorkspaceMember> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersRetrieve(id)
  try {
    return WorkspaceMemberSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesMembersRetrieve\nPath: /api/workspaces/members/{id}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesMembersRetrieve',
            path: '/api/workspaces/members/{id}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PUT
 * @path /api/workspaces/members/{id}/
 */
export async function updateWorkspacesMembersUpdate(  id: string, data: WorkspaceMemberRequest,  client?: any
): Promise<WorkspaceMember> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersUpdate(id, data)
  try {
    return WorkspaceMemberSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`updateWorkspacesMembersUpdate\nPath: /api/workspaces/members/{id}/\nMethod: PUT`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'updateWorkspacesMembersUpdate',
            path: '/api/workspaces/members/{id}/',
            method: 'PUT',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PATCH
 * @path /api/workspaces/members/{id}/
 */
export async function partialUpdateWorkspacesMembersPartialUpdate(  id: string, data?: PatchedWorkspaceMemberRequest,  client?: any
): Promise<WorkspaceMember> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersPartialUpdate(id, data)
  try {
    return WorkspaceMemberSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`partialUpdateWorkspacesMembersPartialUpdate\nPath: /api/workspaces/members/{id}/\nMethod: PATCH`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'partialUpdateWorkspacesMembersPartialUpdate',
            path: '/api/workspaces/members/{id}/',
            method: 'PATCH',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method DELETE
 * @path /api/workspaces/members/{id}/
 */
export async function deleteWorkspacesMembersDestroy(  id: string,  client?: any
): Promise<void> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersDestroy(id)
  return response
}


/**
 * Update member role
 *
 * @method POST
 * @path /api/workspaces/members/{id}/update-role/
 */
export async function createWorkspacesMembersUpdateRoleCreate(  id: string, data: WorkspaceMemberRequest,  client?: any
): Promise<WorkspaceMember> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.membersUpdateRoleCreate(id, data)
  try {
    return WorkspaceMemberSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createWorkspacesMembersUpdateRoleCreate\nPath: /api/workspaces/members/{id}/update-role/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createWorkspacesMembersUpdateRoleCreate',
            path: '/api/workspaces/members/{id}/update-role/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/workspaces/workspaces/
 */
export async function getWorkspacesWorkspacesList(  params?: { ordering?: string; search?: string },  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesList(params?.ordering, params?.search)
  return response
}


/**
 * Create new workspace
 *
 * @method POST
 * @path /api/workspaces/workspaces/
 */
export async function createWorkspacesWorkspacesCreate(  data: WorkspaceCreateRequest,  client?: any
): Promise<Workspace> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesCreate(data)
  try {
    return WorkspaceSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`createWorkspacesWorkspacesCreate\nPath: /api/workspaces/workspaces/\nMethod: POST`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'createWorkspacesWorkspacesCreate',
            path: '/api/workspaces/workspaces/',
            method: 'POST',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method GET
 * @path /api/workspaces/workspaces/{id}/
 */
export async function getWorkspacesWorkspacesRetrieve(  id: string,  client?: any
): Promise<Workspace> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesRetrieve(id)
  try {
    return WorkspaceSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`getWorkspacesWorkspacesRetrieve\nPath: /api/workspaces/workspaces/{id}/\nMethod: GET`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'getWorkspacesWorkspacesRetrieve',
            path: '/api/workspaces/workspaces/{id}/',
            method: 'GET',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PUT
 * @path /api/workspaces/workspaces/{id}/
 */
export async function updateWorkspacesWorkspacesUpdate(  id: string, data: WorkspaceRequest,  client?: any
): Promise<Workspace> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesUpdate(id, data)
  try {
    return WorkspaceSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`updateWorkspacesWorkspacesUpdate\nPath: /api/workspaces/workspaces/{id}/\nMethod: PUT`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'updateWorkspacesWorkspacesUpdate',
            path: '/api/workspaces/workspaces/{id}/',
            method: 'PUT',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method PATCH
 * @path /api/workspaces/workspaces/{id}/
 */
export async function partialUpdateWorkspacesWorkspacesPartialUpdate(  id: string, data?: PatchedWorkspaceRequest,  client?: any
): Promise<Workspace> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesPartialUpdate(id, data)
  try {
    return WorkspaceSchema.parse(response)
  } catch (error) {
    // Zod validation error - log detailed information
    consola.error('❌ Zod Validation Failed');
    consola.box(`partialUpdateWorkspacesWorkspacesPartialUpdate\nPath: /api/workspaces/workspaces/{id}/\nMethod: PATCH`);

    if (error instanceof Error && 'issues' in error && Array.isArray((error as any).issues)) {
      consola.error('Validation Issues:');
      (error as any).issues.forEach((issue: any, index: number) => {
        consola.error(`  ${index + 1}. ${issue.path.join('.') || 'root'}`);
        consola.error(`     ├─ Message: ${issue.message}`);
        if (issue.expected) consola.error(`     ├─ Expected: ${issue.expected}`);
        if (issue.received) consola.error(`     └─ Received: ${issue.received}`);
      });
    }

    consola.error('Response data:', response);

    // Dispatch browser CustomEvent (only if window is defined)
    if (typeof window !== 'undefined' && error instanceof Error && 'issues' in error) {
      try {
        const event = new CustomEvent('zod-validation-error', {
          detail: {
            operation: 'partialUpdateWorkspacesWorkspacesPartialUpdate',
            path: '/api/workspaces/workspaces/{id}/',
            method: 'PATCH',
            error: error,
            response: response,
            timestamp: new Date(),
          },
          bubbles: true,
          cancelable: false,
        });
        window.dispatchEvent(event);
      } catch (eventError) {
        // Silently fail - event dispatch should never crash the app
        consola.warn('Failed to dispatch validation error event:', eventError);
      }
    }

    // Re-throw the error
    throw error;
  }
}


/**
 * API operation
 *
 * @method DELETE
 * @path /api/workspaces/workspaces/{id}/
 */
export async function deleteWorkspacesWorkspacesDestroy(  id: string,  client?: any
): Promise<void> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesDestroy(id)
  return response
}


/**
 * List workspace members
 *
 * @method GET
 * @path /api/workspaces/workspaces/{id}/members/
 */
export async function getWorkspacesWorkspacesMembersRetrieve(  id: string,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesMembersRetrieve(id)
  return response
}


/**
 * Get workspace statistics
 *
 * @method GET
 * @path /api/workspaces/workspaces/{id}/stats/
 */
export async function getWorkspacesWorkspacesStatsRetrieve(  id: string,  client?: any
): Promise<any> {
  const api = client || getAPIInstance()
  const response = await api.workspaces_workspaces.workspacesStatsRetrieve(id)
  return response
}


